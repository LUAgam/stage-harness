---
description: 保持 mmr 子代理调度稳定、单层、可控，并支持单次任务串行修复与复审
alwaysApply: true
---

# MMR Subagent 使用规则

1. 每个阶段只允许调用当前明确指定的 custom subagent。
2. 禁止启动嵌套 subagent。
3. 除非用户明确要求，否则不要启动内置 subagents（如 Explore、Browser、Bash）。
4. 不要依赖 Task tool 的动态 model 参数做模型路由。
5. 如果当前指定的 custom subagent 不可用，则回退到主 Agent 执行，并明确说明发生了回退。
6. 所有 review 类 subagent 都不能修改代码。
7. executor 只能按已确认方案执行，除非被明确阻塞，否则不能擅自重做设计。
8. 如果发现阻塞项，要明确指出阻塞原因、影响范围、建议处理方式，不要静默换方案。
9. review 结论必须尽量具体，优先指出高风险问题，不要泛泛而谈。
10. 所有输出优先使用中文，保留必要的英文技术名词。

# 当前流程约束

本套流程分为以下阶段：

1. 方案制定：由 mmr-planner 负责
2. 方案复审：由 mmr-plan-reviewer 负责
3. 执行落地：由 mmr-executor 负责
4. 代码复审 A：由 mmr-code-reviewer-gpt 负责
5. 代码复审 B：由 mmr-code-reviewer-gemini 负责

# 单次任务默认执行流程

当用户请求“修复 bug / 修复问题 / 一次性完成修复和 review / 不要频繁交互”时，主 Agent 默认按以下顺序自动执行，不需要逐步等待用户确认：

1. 调用 mmr-planner，分析问题并给出最小修复方案
2. 调用 mmr-plan-reviewer，对方案进行复审
3. 如果方案没有 P0 阻塞，则调用 mmr-executor 执行代码修改
4. 执行完成后，调用 mmr-code-reviewer-gpt 做第一轮代码复审
5. 再调用 mmr-code-reviewer-gemini 做第二轮代码复审
6. 最终统一输出结果给用户

# 何时允许中途停止并请求用户确认

只有以下情况才允许中断并向用户确认：

1. 方案复审发现 P0 风险，且无法在小范围内收敛
2. 执行阶段遇到高风险阻塞，需要改动范围明显扩大
3. 需要删除、重构、迁移核心逻辑，且影响面较大
4. 缺少关键上下文，继续执行会明显增加误改概率
5. 测试、构建、运行结果表明修复方向可能错误

除以上情况外，应默认继续执行完整流程，不要频繁请求用户确认。

# 最终输出格式要求

最终回复必须统一包含以下内容：

## 问题理解
## 根因分析
## 最终修复方案
## 实际改动文件
## 校验结果
## GPT 复审结论
## Gemini 复审结论
## 最终风险与建议
## 是否建议提交
