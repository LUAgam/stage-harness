---
description: "MMR 快速修复流程（方案 -> 方案复审 -> 执行 -> A/B 代码复审）"
argument-hint: "<需求或 bug 描述>"
---

# mmr

Claude Code 版 MMR 入口命令。用于不需要完整 `.harness` Epic 状态机、但需要“先方案、再复审、再执行、最后双视角 review”的快速修复或小型改动。

## 适用场景

- 修复明确 bug。
- 小范围功能调整。
- 用户要求“一次性完成修复和 review，不要频繁交互”。
- 不希望启动完整 `/stage-harness:harness-start` 到 `/stage-harness:harness-done` 流水线，但仍希望有方案和复审约束。

## 不适用场景

- 大型跨模块重构。
- 需要正式需求澄清、规格、任务 DAG、验收议会的 Epic。
- 高风险数据迁移、权限模型重做、核心协议变更。
- 缺少关键上下文，继续执行会明显增加误改概率。

这些场景应使用完整 stage-harness 流程。

## 角色映射

| 阶段 | Claude Code agent | 责任 |
|------|-------------------|------|
| 方案制定 | `mmr-planner` | 输出最小可执行方案 |
| 方案复审 | `mmr-plan-reviewer` | 检查方案遗漏、P0/P1/P2 风险 |
| 执行落地 | `mmr-executor` | 按已确认方案做最小必要改动 |
| 代码复审 A | `mmr-code-reviewer-a` | 正确性、回归风险、测试缺口 |
| 代码复审 B | `mmr-code-reviewer-b` | 边界、契约、一致性、隐性风险 |

说明：Claude Code 插件内的 agent 使用 `model: inherit`。这里的 A/B 是复审视角隔离，不承诺真实调用 GPT/Gemini。如需真实多模型，需要额外接入外部模型命令或 MCP。

## 执行流程

### Step 1：主会话收集最小上下文

在调用 agent 前，先收集必要上下文：

```bash
git status --short
git diff --stat
```

根据用户输入和当前仓库，读取与问题最相关的文件。不要全仓无边界扫描。

### Step 2：调用 `mmr-planner`

向 `mmr-planner` 传入：
- 用户需求或 bug 描述：`$ARGUMENTS`
- 已收集的相关上下文
- 当前 git 状态摘要

要求其输出可执行方案、影响范围、风险、回滚点和验证方案。

### Step 3：调用 `mmr-plan-reviewer`

向 `mmr-plan-reviewer` 传入：
- 用户需求
- `mmr-planner` 输出
- 主会话认为重要的上下文

如果复审发现 P0 且无法小范围收敛，停止并向用户说明阻塞。若只有 P1/P2，主会话可将修正要求并入执行提示继续推进。

### Step 4：调用 `mmr-executor`

仅在方案无 P0 阻塞时调用。传入：
- 用户需求
- 最终方案
- 方案复审结论
- 明确的范围边界和验证要求

执行 agent 只能按方案做最小必要改动。执行完成后，主会话读取 `git diff --stat` 和必要 diff，为复审准备输入。

### Step 5：调用 `mmr-code-reviewer-a`

传入：
- 用户需求
- 最终方案
- 执行摘要
- 本次 diff
- 校验结果

要求从正确性、回归风险、测试缺口和方案一致性角度审查。

### Step 6：调用 `mmr-code-reviewer-b`

传入：
- 用户需求
- 最终方案
- 执行摘要
- 本次 diff
- 代码复审 A 结论
- 校验结果

要求从边界、契约、一致性、错误处理和隐性风险角度审查。

### Step 7：最终汇总

最终回复必须包含：

```markdown
## 问题理解
## 根因分析
## 最终修复方案
## 实际改动文件
## 校验结果
## 复审 A 结论
## 复审 B 结论
## 最终风险与建议
## 是否建议提交
```

## 中途暂停条件

只有以下情况允许中途停止并请求用户确认：

1. 方案复审发现 P0 风险，且无法在小范围内收敛。
2. 执行阶段遇到高风险阻塞，需要明显扩大改动范围。
3. 需要删除、重构、迁移核心逻辑，且影响面较大。
4. 缺少关键上下文，继续执行会明显增加误改概率。
5. 测试、构建、运行结果表明修复方向可能错误。

其余情况默认继续跑完整个 MMR 流程，最后一次性汇总。
